import 'package:flutter/material.dart';

class UsuariosAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Cuanto deben los usuarios'),
      ),
    );
  }
}
